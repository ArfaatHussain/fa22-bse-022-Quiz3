pipeline {
    agent any

    stages {
        stage('Run Python Script') {
            steps {
                echo "Running Python Program"
                sh 'python3 app.py'
            }
        }
    }
}
